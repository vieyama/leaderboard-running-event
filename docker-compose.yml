services:
  laravel:
    build: .
    container_name: laravel-app
    ports:
      - "3003:3003"
    volumes:
      # Mount only what we need so vendor is kept inside container
      - ./storage:/var/www/storage
      - ./.env:/var/www/.env
